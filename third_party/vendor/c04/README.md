# C04 vendor area

No third-party code is vendored by C04. If a future approved exception copies
code here, retain origin, immutable commit/tag, SPDX notice, upstream diff and
equivalence/integration tests in `docs/comercial/c04/reuse.json` before it is
distributed.
