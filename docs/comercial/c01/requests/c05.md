# Pedido mínimo a C05 (não implementado por C01)

C01 precisa, no módulo `modules/qualification_profile/`:

```python
def assess_qualification(context: Mapping, profile: Mapping) -> Mapping:
    """Return rule_results + optional intervals. Never called to invent passed."""
```

Campos usados por C01:

- `rule_results`: lista RuleResult MP-QUAL/1
- `intervals.arbitration_interval` / `intervals.admissible_interval` (opcional)
- catálogo `get_profile(profile) -> bool`

Teste de fumaça: um perfil `market.urban.comparative` e um `insurance.habitacional.reconstruction` com `value_basis` distinto. Sem isso, C01 marca `unverified` e bloqueia `ready_for_professional_signoff`.
